{
  "name": "@pool-drill-gen/cli",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "pooldrill": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "typecheck:test": "tsc -b tsconfig.test.json",
    "test": "pnpm run typecheck:test && vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@pool-drill-gen/core": "workspace:*",
    "@pool-drill-gen/schema": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3",
    "vite": "^6",
    "vitest": "^4"
  }
}
