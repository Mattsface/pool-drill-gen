#!/usr/bin/env node
// Placeholder entry point for @pool-drill-gen/cli.
// `validate` and `show` commands land in later M1 issues.
import { CORE_PACKAGE_PLACEHOLDER } from "@pool-drill-gen/core";

if (CORE_PACKAGE_PLACEHOLDER) {
  console.log("pool-drill-gen CLI: workspace bootstrap complete");
}
