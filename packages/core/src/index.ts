// Placeholder entry point for @pool-drill-gen/core.
// Platform-neutral: no fs, no process, no node:* imports.
// Coordinate/diamond helpers and validateDrill() land in later M1 issues.
import { SCHEMA_PACKAGE_PLACEHOLDER } from "@pool-drill-gen/schema";

export const CORE_PACKAGE_PLACEHOLDER = SCHEMA_PACKAGE_PLACEHOLDER;
